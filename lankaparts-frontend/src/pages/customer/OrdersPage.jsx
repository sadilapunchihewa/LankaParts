import { Link } from 'react-router-dom'
import OrderStatusBadge from '../../components/customer/OrderStatusBadge'
import { orders } from '../../data/marketplace'
import { formatLKR } from '../../utils/currency'
export default function OrdersPage() { return <><header className="account-heading"><span>ORDER HISTORY</span><h1>My Orders</h1><p>Review and track all your LankaParts purchases.</p></header><section className="account-panel"><div className="panel-heading"><div><h2>All orders</h2><p>{orders.length} marketplace orders</p></div><select aria-label="Filter orders"><option>All statuses</option><option>Pending</option><option>Confirmed</option><option>Processing</option><option>Shipped</option><option>Delivered</option><option>Cancelled</option></select></div><div className="orders-table"><div className="order-table-head"><span>Order</span><span>Date</span><span>Status</span><span>Total</span><span></span></div>{orders.map((order) => <Link to={`/account/orders/${order.id}`} className="order-row" key={order.id}><strong>#{order.id}</strong><span>{order.date}<small>{order.items} items</small></span><OrderStatusBadge status={order.status} /><strong>{formatLKR(order.total)}</strong><span>Details →</span></Link>)}</div></section></> }

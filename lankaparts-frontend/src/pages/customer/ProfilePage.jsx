import { Mail, Phone, UserRound } from 'lucide-react'
import { toast } from 'sonner'
import { useAuth } from '../../contexts/AuthContext'
export default function ProfilePage() { const { user } = useAuth(); return <><header className="account-heading"><span>PERSONAL INFORMATION</span><h1>Profile Details</h1><p>Keep your contact details accurate for orders and delivery.</p></header><form className="account-panel profile-form" onSubmit={(e) => { e.preventDefault(); toast.success('Profile changes saved.') }}><div className="form-grid"><label className="form-field"><span>First name</span><div className="input-shell"><UserRound /><input defaultValue={user.firstName} /></div></label><label className="form-field"><span>Last name</span><div className="input-shell"><UserRound /><input defaultValue={user.lastName} /></div></label></div><label className="form-field"><span>Email</span><div className="input-shell"><Mail /><input type="email" defaultValue={user.email} disabled /></div></label><label className="form-field"><span>Phone number</span><div className="input-shell"><Phone /><input placeholder="+94 77 123 4567" /></div></label><button className="auth-submit">Save changes</button></form></> }
